<li class="featured-card {{post_class}}">
  <a class="card-link" href="{{url}}">
    <figure class="card-media">
      {{#if feature_image}}
        <img src="{{img_url feature_image size="m"}}" alt="{{title}}" />
      {{/if}}
    </figure>
    <div class="card-title">{{title}}</div>
  </a>
</li>