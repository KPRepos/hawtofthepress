<ul class="nav-menu">
  {{#foreach navigation}}
    <li class="nav-item {{#if current}}nav-current{{/if}} nav-{{slug}}">
      <a href="{{url absolute="true"}}" class="nav-link" {{#if current}}aria-current="page"{{/if}}>{{label}}</a>
    </li>
  {{/foreach}}
</ul>


