{{!< default}}
{{#author}}
  <header class="archive-header">
    <h1>{{name}}</h1>
    {{#if bio}}<p class="subtle">{{bio}}</p>{{/if}}
  </header>
{{/author}}
<section class="list">
  <ul class="feed" id="feed">
    {{#foreach posts}}
      <li class="{{post_class}}">
        <time
          class="date"
          datetime="{{date format="YYYY-MM-DD"}}"
        >{{date}}</time>
        —
        <a href="{{url}}">{{title}}</a>
        {{#if tags}}<div class="tags">#
            {{tags separator=" # " autolink="true"}}</div>{{/if}}
      </li>
    {{/foreach}}
  </ul>
  {{pagination}}
</section>